i think the most useful part is AST, because it allows us to swap syntax implementations without changing code. in languages most useful is C++ practice, just writing code, and refactoring it.

KaguOS forgotten

LLVM, that is basically taken assembly compilation and that it based on C--, that compilers can be the same but languages differs, and last but not the least - i can create most diabolical, unconventional and esoteric syntax and it will be Turing-complete.

reflections and memory management by compiler

add optional task to compile your language to Kagu